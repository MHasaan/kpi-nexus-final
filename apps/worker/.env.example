NODE_ENV=development
LOG_LEVEL=info

# Redis is the BullMQ broker
REDIS_URL=redis://localhost:6379

# Database (worker writes to the same Postgres as api)
DATABASE_URL=postgresql://kpi_nexus:dev_password@localhost:5432/kpi_nexus
